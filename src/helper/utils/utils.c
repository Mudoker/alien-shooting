#include "utils.h"
#include "../../../assets/fonts/font.h"

// Compare two strings
boolean is_equal(char *str1, char *str2) { return False; }

// Get the length of the string
int len(const char *str) { return 0; }

// Concatenate two strings
boolean concat(char *dest, const char *src) { return False; }

// Copy the source string to the destination string
void copy(char *destination, const char *source) {}

// Check if the string starts with the prefix
boolean starts_with(char *str, char *prefix) { return False; }

// Convert an integer to a string
void int_to_string(int number, char *result) {}

// Convert an integer to a hexadecimal string
char *hex_to_string(unsigned int number) { return ""; }

// Convert a string to an integer
int string_to_int(char *charArray) { return 0; }

// Convert a string to a hexadecimal integer
void mac_address_format(unsigned int num1, unsigned int num2,
                        char *mac_address) {}